# Application
PORT=8080
NODE_ENV=development

# CORS - URL del frontend para permitir peticiones
FRONTEND_URL=http://localhost:3000

# JWT - Configuración del token JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database - Credenciales para la conexión con PostgreSQL
# Estos valores por defecto coinciden con los del docker-compose.yml
DB_HOST=db
DB_PORT=5432
DB_USERNAME=unishop_user
DB_PASSWORD=unishop_password
DB_NAME=unishop_db

# Logging
LOG_LEVEL=info